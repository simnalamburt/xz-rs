language: rust
os:
  - linux
  - osx
install:
  - sudo apt-get install make automake autotools-dev
notifications:
  irc:
    nick: xz-rs
    channels:
      - irc.uriirc.org:16667#hyeon
    template:
      - "#%{build_number} %{result}, %{build_url}"
    skip_join: true
