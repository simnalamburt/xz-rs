sudo: false
language: rust
rust:
  - stable
  - beta
  - nightly
os:
  - linux
  - osx


notifications:
  irc:
    nick: xz-rs
    channels:
      - irc.uriirc.org:16667#hyeon
    template:
      - "#%{build_number} %{result}, %{build_url}"
    skip_join: true
